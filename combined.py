# import writer function
from model_config import ModelConfig
# both of these functions should take the writer as a param so that you can write to it within the function
# from updated_steady import build_steady_topo
# import fault function


# define writer 
        
        
        
    
config = ModelConfig()

print(config.model_name)
print(config.dt_steady)